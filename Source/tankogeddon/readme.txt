8 lesson:
    По основному заданию: пукнты все готовы. 
    3-й пункт сделал через криввые безье. Также сделал настройку траектории полёта (она постоянна) и
    настройку скорости полёта (HighestSpeedAdjuster отображает максимальную скорость, которую снаряд 
    достигнет в конце траектории. И SlowestSpeedAdjuster - минимальная скорость, достигаемая в середине
    полёта)

    Дополнительные изменения: полностью переработанна архитектура проекта, теперь, при добавление
    ещё каких-то классов, нет необходимости повторно реализовать одну и ту же логику интерфейсов.
    Башни, корпуса и пушки вынесены в другую иерархию с такими же целями.

    Баги: пофикшенно огромное количетсов багов, но меньше их не стало :D 
    Основной баг - это пулл снарядов. Не знаю почему, но он не хочет сравнивать два TSubClassof`а,
    указывающие на один и тот же базовый класс. AProjectile - базовый класс и от него наследуется
    APhysicsProjectile. Но почему-то сравнивания их TSubClassof от AProjectile в какой-то момент
    выскакивает (illegal name (lenght > MAX_SIZE)) ну и исключение, само собой. Ничего в интернете 
    по такому поводу не нашёл и способов безопасно сравнивать два класс тоже